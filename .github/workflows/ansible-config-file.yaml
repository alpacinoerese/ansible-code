name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u al bash -c 'cat > /home/al/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/al/ansible/inventory.yaml
          roles_path    = /home/al/ansible/roles
          host_key_checking = False
          remote_user = al
          private_key_file = /home/al/.ssh/id_rsa
          log_path = /home/al/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/al/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/al/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown al:al /home/al/ansible/ansible.cfg
          sudo chmod 644 /home/al/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u al ls -la /home/al/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u al cat /home/al/ansible/ansible.cfg
